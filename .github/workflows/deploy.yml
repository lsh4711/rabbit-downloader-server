name: deploy

on:
  push:
    branches:
      - main

#      test
      - develop

jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: run
        run: ls -l
